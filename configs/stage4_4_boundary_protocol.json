{
  "protocol_schema_version": "aic.stage4-boundary-experiment-protocol/v1",
  "protocol_status": "DRAFT_FOR_INDEPENDENT_REVIEW",
  "stage": "4.4",
  "stage_title": "Temporal Boundary Refinement Baseline",
  "research_question": "For frozen candidates whose event already exists, does restricted local refinement of start_sec/end_sec only improve Temporal IoU without violating Recall Guardrails?",
  "hypothesis_status": "Boundary refinement is the currently most worthwhile next bottleneck hypothesis from Stage 4.1 (boundary-involved 14/36, over_wide 11/36) plus Stage 4.3 (Selection family exhausted); it is NOT a proven effect.",
  "source_cache_global_hash": "4b515a6d6fb47073413c686214c3fa5f97293655a3824241eb3305b9e7753246",
  "role_manifest_summary_hash": "6eccf82509295980cb16270a306fad251941b6a09f9f0178335eeabd811b23e0",
  "role_manifest_hashes": {
    "dev_tune": "bceb82034bcc5eed42a5099d7f54dd0fa776fe240969dfada231d255a368ec71",
    "hard_stress": "81d734ee28ec38c096b908a573249c62713d358fb03ea135e5b55e1ae4405687",
    "audit_diagnostic": "98fd987b7b56bdc18dd58a0c809e77adb28b93e322fd08a5246c04e115e1780c"
  },
  "data_roles": {
    "dev_tune": "Only role allowed for development smoke and, if a parameter grid is ever preregistered, Dev-only parameter selection",
    "hard_stress": "Frozen-method stress evaluation only, after method freeze and never before",
    "audit_diagnostic": "Post-hoc diagnostic only, after Hard is complete; never for Prompt/threshold/padding/rule development",
    "heldout": "ABSOLUTELY_LOCKED"
  },
  "git_head_policy": "Git HEAD is recorded in freeze/overall_method_freeze.json at method-freeze time, not embedded in this preregistration payload to avoid commit circularity.",
  "refiners": {
    "BR-0": {
      "definition": "Identity boundary control: refined_start_sec = original start_sec and refined_end_sec = original end_sec for every candidate; no model, no video, no parameters",
      "refiner_version": "aic.temporal-boundary-refinement/v1",
      "parameters": {}
    },
    "BR-1": {
      "definition": "Local Semantic Temporal Boundary Refiner: for each frozen merged candidate, build a clamped local context window around the candidate, show the model only that local clip, and ask for the same event's refined start/end; any parse, schema, guard, or model fallback outcome reverts to the original boundaries",
      "refiner_version": "aic.temporal-boundary-refinement/v1",
      "parameters": {
        "context_padding_sec": 20.0,
        "max_context_duration_sec": 120.0,
        "min_parent_temporal_iou": 0.5
      },
      "parameter_policy": "Fixed conservative values for v1; not tuned on Audit36 or Smoke metrics. If a grid is ever introduced it must be preregistered here first and selected on Dev-Tune166 only.",
      "model": {
        "model_name": "Qwen/Qwen3.5-4B",
        "backend": "existing externally managed vLLM OpenAI-compatible server; no new inference framework",
        "temperature": 0.0,
        "max_new_tokens": 256,
        "enable_thinking": false
      },
      "prompt_version": "boundary_local_context_v0_draft",
      "prompt_provenance": {
        "source": "Generic boundary-refinement task description only; no Stage 4.1 per-video adjudication content, no video-specific rules, no audit rationales",
        "prohibited_sources": ["Stage 4.1 adjudication labels or rationales", "Audit36 per-video rules", "Smoke metric-driven prompt edits after Formal begins"]
      }
    }
  },
  "local_context_policy": {
    "window_definition": "window = [max(0, start - context_padding_sec), min(duration, end + context_padding_sec)]; if the padded window exceeds max_context_duration_sec the excess is trimmed deterministically from the larger outer side first while always keeping the candidate interval fully inside",
    "input_scope": "The model may only observe the extracted local clip; full-video re-entry is forbidden",
    "coordinate_system": "The model returns seconds relative to the clip start; the refiner maps them back by adding the window start",
    "clip_extraction": "extract_video_clip (ffmpeg preferred, OpenCV fallback); source videos are read-only"
  },
  "event_identity_guard": {
    "purpose": "Refinement must stay the same event as the parent candidate; moving to a neighbouring event is forbidden",
    "checks": [
      "interval_valid: refined interval is finite, 0 <= refined_start < refined_end <= video duration",
      "within_local_window: refined interval lies inside the recomputed local context window (1e-9 tolerance)",
      "parent_temporal_iou: temporal_iou(original interval, refined interval) >= min_parent_temporal_iou"
    ],
    "on_failure": "Reject the model output and fall back to the original boundaries; the rejection reason and guard detail are recorded; nothing is silently repaired into a different event"
  },
  "fallback_policy": {
    "triggers": [
      "model transport or empty response",
      "response does not parse as the exact required JSON schema",
      "model outputs decision IDENTITY_FALLBACK",
      "model outputs boundaries identical to the original (no-op refine is recorded as REFINE with equal values)",
      "event identity guard failure"
    ],
    "behaviour": "IDENTITY_FALLBACK keeps the original start/end; candidate is never dropped, never created, never split, never merged"
  },
  "allowed_boundary_modifications": [
    "start_sec of an existing frozen merged candidate",
    "end_sec of an existing frozen merged candidate"
  ],
  "forbidden_operations": [
    "add, delete, split, or merge candidates",
    "candidate selection, reranking, or budgeting (Stage 4.3 scope, closed)",
    "modify score, reason, source, source_chunk, or any provenance field (structurally impossible: replay projects these from the frozen cache)",
    "re-run retrieval or a second-pass highlight search over the whole video",
    "read Heldout in any form",
    "read Audit36 adjudication before Hard is complete",
    "use Audit36 observations to tune Prompt, padding, thresholds, or rules",
    "alter SEL-3 frozen rules or config hash 7009da355490267818c7c01e0407874e79869ecefe3515125eea10ac1a072d7a"
  ],
  "metric_definitions": {
    "implementation": "aic_video_highlight.highlight_retrieval.baseline_experiment.evaluate_weak_references (frozen Stage 3 weak-reference evaluator, unchanged)",
    "primary": "mean Temporal IoU",
    "secondary": ["mean weak-reference F1", "mean weak-reference Precision", "mean prediction coverage", "total over-prediction seconds", "missed-reference fraction"],
    "status": "weak-reference development metrics; NOT official competition metrics",
    "evaluation_schema_reuse": "Evaluation and comparison reuse aic.candidate-selection-evaluation/v1 and aic.candidate-selection-comparison/v1 because the frozen metric contract is identical; the paired baseline is the same role's BR-0 identity evaluation."
  },
  "dev_recall_guardrail": {
    "min_mean_recall": 0.94,
    "min_delta_mean_recall": -0.02,
    "max_delta_missed_reference_fraction": 0.02,
    "max_delta_empty_prediction_count": 0,
    "max_delta_recall_eq_0_count": 0,
    "max_delta_recall_lt_0_5_count": 1,
    "max_delta_recall_lt_0_8_count": 3
  },
  "dev_promotion_gate": {
    "min_delta_mean_temporal_iou": 0.01,
    "min_delta_mean_f1": 0.0
  },
  "hard_stress_gate": {
    "min_delta_mean_recall": -0.03,
    "max_delta_missed_reference_fraction": 0.03,
    "max_delta_empty_prediction_count": 0,
    "max_delta_recall_eq_0_count": 0,
    "max_delta_recall_lt_0_5_count": 2,
    "max_delta_recall_lt_0_8_count": 5,
    "min_delta_mean_f1": 0.0,
    "min_delta_mean_precision": 0.0,
    "min_delta_mean_temporal_iou": 0.0,
    "max_delta_total_over_prediction_sec": 0.0
  },
  "gate_discipline": "Gates must be applied exactly as written; a Recall Guardrail failure blocks Promotion regardless of Temporal IoU; no gate may be relaxed because Stage 4.3 was a negative result.",
  "method_freeze_rule": "BR-1 Prompt version, parameters, and gates are frozen in an overall_method_freeze.json artifact before any Hard-Stress record is read; the freeze artifact records hard_results_observed=false, audit_results_observed=false, heldout_accessed=false.",
  "parameter_selection_rule": {
    "eligibility": "BR-1 v1 uses fixed protocol parameters; there is no Dev grid in this version. If any future version introduces a grid, eligibility is passing every dev_recall_guardrail check and selection happens on Dev-Tune166 only.",
    "no_eligible_result": "NO_ELIGIBLE_CONFIGURATION; do not relax gates"
  },
  "formal_phases": [
    "Phase 0 validate Git, frozen cache, role manifests, and protocol hashes; freeze protocol status to PREREGISTERED_BEFORE_FORMAL after independent review acceptance",
    "Phase 1 run BR-0 identity control on Dev-Tune166 and verify exact replay equivalence with the frozen baseline",
    "Phase 2 run frozen BR-1 on Dev-Tune166",
    "Phase 3 write overall_method_freeze evidence before Hard",
    "Phase 4 run frozen BR-1 on Hard-Stress229 without feedback",
    "Phase 5 apply preregistered promotion and Hard-Stress gates",
    "Phase 6 only then permit Audit36 post-hoc diagnostic",
    "Phase 7 statistical analysis, figures, and Markdown report"
  ],
  "stopping_rules": [
    "Stop on any cache, role, protocol, refinement, replay, or evaluation hash mismatch",
    "Stop if candidate count or candidate identity changes anywhere in the chain",
    "Stop if any replayed score/reason/source differs from the frozen cache",
    "Stop on reference/metric/Audit-label leakage into refinement inputs or outputs",
    "Stop on Heldout access",
    "Do not alter Prompt, parameters, guardrails, or gates after Formal begins"
  ],
  "development_smoke_policy": {
    "labels": ["DEVELOPMENT_SMOKE", "NON_FORMAL", "NOT_FOR_PARAMETER_SELECTION"],
    "maximum_video_count": 8,
    "allowed_role": "dev_tune only",
    "purpose": "Engineering plumbing only: BR-0 identity, BR-1 schema/fallback/validator, determinism; Smoke metrics must never be used to tune BR-1",
    "must_not_change_protocol": true
  },
  "audit_posthoc_policy": "Audit36 stays locked until Phases 1-5 are complete; Audit output is diagnostic only and cannot change any frozen decision.",
  "protocol_semantic_sha256": "82aef34d8509a0b4c6f85e7acf06122d03c9ccd50701ca4008f9450b391ce963"
}
